import Button from '@mui/material/Button'

const Buttonform = () => {
    return(
        <Button variant = "outlined" color = "error">ボタンの名前</Button>
        // colorがsuccess(緑)、error(赤)
    )
}

export default Buttonform
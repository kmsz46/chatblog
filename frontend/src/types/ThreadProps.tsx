import React, { useState } from 'react'

type ThreadPropsType = {
    title:string;
    content:string;
    article_user:string;
    tag:string;
    group:string;
}

export default ThreadPropsType
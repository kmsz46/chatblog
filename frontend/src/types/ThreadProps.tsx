import React, { useState } from 'react'

type ThreadPropsType = {
    title:string;
    content:string;
    article_user:string;
    tag:Array<string>;
    group:Array<string>;
}

export default ThreadPropsType